给定 A、B 两个文件，各存放 50 亿个 URL，每个 URL 各占 64B，内存限制是 4G。请找出 A、B 两个文件共同的 URL

每个 URL 占 64B，那么 50 亿个 URL占用的空间大小约为 320GB,所以不可能将数据一次性写入内存
所以采用分治策略：把一个文件中的 URL 按照某个特征划分为多个小文件，使得每个小文件大小不超过 4G，然后在内存里进行处理
首先遍历文件 A并对文件里的url进行hash(url) % 1000分为1000个小文件，这样每个文件大小大概为300M,同样对文件B进行
hash处理分为1000个小文件，因为使用的hash函数一样，所以两个文件中相同的url会落在同样下标文件里，比如相同的xxx.com
如果落在了A3,那也一定会落在B3,
这样我们先将Ai放进行hashSet里，然后看对应的Bi文件里是否有url存在hashSet里，有的话就是相同

这里重点是使用相同的hash函数对两个文件进行运算，这样相同url会落在相同下标小文件里

